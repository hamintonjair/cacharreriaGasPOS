# Variables de entorno para producción en Render
NODE_ENV=production
PORT=5000

# La URL de la base de datos se configurará automáticamente en Render
# a través del archivo render.yaml usando fromDatabase

# JWT Secret se generará automáticamente en Render
# a través del archivo render.yaml usando generateValue
